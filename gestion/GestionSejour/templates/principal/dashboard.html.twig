{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
<div class="dashboard-container">
    <div class="dashboard-header">
        <h1>Bienvenue {{ user.username }}</h1>
        <p>Gestion des séjours et des patients</p>
    </div>

    <div class="dashboard-grid">
        {% if is_granted('ROLE_ADMIN') %}
            <div class="dashboard-card">
                <div class="card-icon">👥</div>
                <h3>Gestion Patients</h3>
                <p>Gérer les informations des patients</p>
                <a href="{{ path('gestion_patients') }}" class="card-link">Accéder</a>
            </div>
            <div class="dashboard-card">
                <div class="card-icon">🏥</div>
                <h3>Gestion Séjours</h3>
                <p>Gérer les séjours des patients</p>
                <a href="{{ path('gestion_sejours') }}" class="card-link">Accéder</a>
            </div>
        {% endif %}

        {% if is_granted('ROLE_INFIRMIER') %}
            <div class="dashboard-card">
                <div class="card-icon">➡️</div>
                <h3>Arrivée Patient</h3>
                <p>Enregistrer l'arrivée d'un patient</p>
                <a href="{{ path('arrivee_patient') }}" class="card-link">Accéder</a>
            </div>
            <div class="dashboard-card">
                <div class="card-icon">⬅️</div>
                <h3>Sortie Patient</h3>
                <p>Enregistrer la sortie d'un patient</p>
                <a href="{{ path('sortie_patient') }}" class="card-link">Accéder</a>
            </div>
            <div class="dashboard-card">
                <div class="card-icon">📋</div>
                <h3>Consulter Séjours</h3>
                <p>Voir tous les séjours</p>
                <a href="{{ path('consultation_sejours') }}" class="card-link">Accéder</a>
            </div>
            <div class="dashboard-card">
                <div class="card-icon">📅</div>
                <h3>Séjours à venir</h3>
                <p>Voir les séjours programmés</p>
                <a href="{{ path('sejours_a_venir') }}" class="card-link">Accéder</a>
            </div>
        {% endif %}

        {% if is_granted('ROLE_MEDECIN') %}
            <div class="dashboard-card">
                <div class="card-icon">🔍</div>
                <h3>Consultation Séjours</h3>
                <p>Consulter les séjours des patients</p>
                <a href="{{ path('consultation_sejours') }}" class="card-link">Accéder</a>
            </div>
        {% endif %}
    </div>
</div>

{% endblock %}
